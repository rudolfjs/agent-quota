## 0.3.5 - 2026-04-23
### Changed
* deps: bump the go-dependencies group
### Fixed
* installer: extract release archive via subshell+relative path so non-GNU tar variants no longer fail with duplicated-path "Cannot open" errors (#63)
