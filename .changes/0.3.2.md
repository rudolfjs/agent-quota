## 0.3.2 - 2026-04-17
### Added
* New `.devcontainer/` (Go 1.25) and `scripts/e2e-smoke.sh` — catches CLI regressions unit tests miss
### Fixed
* self-update: preserve the executable bit on the installed binary — previously non-executable after update
